# ---------- QAQandA – Environment Example ----------
# Copy to `.env.local` and set your values.
# In PRODUCTION, Databricks host + token are REQUIRED.

NODE_ENV=development

# Databricks SQL Warehouse
DATABRICKS_HOST=https://workspace.cloud.databricks.com
DATABRICKS_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Canary integration (EP03)
DATABRICKS_CANARY_ENABLED=false
DATABRICKS_RO_TOKEN=read_only_placeholder

# OpenAI (optional now)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Session secret must be VALID LENGTH (>=32 chars)
SESSION_SECRET=demo_session_secret_key_that_is_long_enough_123
